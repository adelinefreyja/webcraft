{% extends 'backoffice/index.html.twig' %}

{% block specificcss %}
{% endblock %}


{% block content %}
<div class="container">
	<div class="row">
        {{ form_start(form1) }}
            <div class="input-field">
                {{ form_label(form1.productName) }}
                {{ form_widget(form1.productName) }}
            </div>
            <div class="input-field">
                {{ form_label(form1.productDescription) }}
                {{ form_widget(form1.productDescription) }}
            </div>
            <div class="input-field">
                {{ form_label(form1.productPrice) }}
                {{ form_widget(form1.productPrice) }}
            </div>
            <div class="input-field">
                {{ form_label(form1.productSale) }}
                {{ form_widget(form1.productSale) }}
            </div>
        {{ form_start(form2) }}
             <div class="file-field input-field">
                 <div class="btn">
                     <span>File</span>
                     {{ form_widget(form2.image) }}
                 </div>
                 <div class="file-path-wrapper">
                     <input class="file-path validate" type="text">
                 </div>
            </div>
            <div class="right">
                <button type="submit" class="waves-effect waves-light btn btn-success orange lighten-2"><i class="fas fa-paper-plane fa-lg"></i> Valider</button>
            </div>
        {{ form_end(form2) }}
        {{ form_end(form1) }}
	</div>
</div>
{% endblock %}

{% block specificjs %}
{% endblock %}
