{% extends ('setup/setup.html.twig') %}

{% block nav %}
	<div class="row">
		<div class="setupNav center-align">
			<a href="{{ path('setup') }}" class="btn-floating btn-large waves-effect waves-light orange lighten-2"><i class="fas fa-database fa-lg active"></i></a>............
			<a href="{{ path('setup2') }}" class="btn-floating btn-large waves-effect waves-light grey lighten-2"><i class="fas fa-tv fa-lg inactive"></i></a>............
			<a href="{{ path('setup3') }}" class="btn-floating btn-large waves-effect waves-light grey lighten-2"><i class="far fa-user fa-lg inactive"></i></a>
		</div>
	</div>
{% endblock %}

{% block content %}
{% block contentTitle %}
	<div class="title center-align">
		<i class="fas fa-database fa-3x"></i>
		<h4> Etape 1 :: Votre base de données</h4>
		<hr>
	</div>
{% endblock %}

	{{ form_start(form) }}
		<div class="input-field">
			{{ form_label(form.db_host) }}
			{{ form_widget(form.db_host) }}
		</div>
		<div class="input-field">
			{{ form_label(form.db_username) }}
			{{ form_widget(form.db_username) }}
		</div>
		<div class="input-field">
			{{ form_label(form.db_password) }}
			{{ form_widget(form.db_password) }}
		</div>
		<div class="input-field">
			{{ form_label(form.db_name) }}
			{{ form_widget(form.db_name) }}
		</div>
		<div class="right">
			<button type="submit" class="waves-effect waves-light btn btn-success orange lighten-2"><i class="fas fa-paper-plane fa-lg"></i> Valider</button>
		</div>
	{{ form_end(form) }}

{% endblock %}